import React from 'react';

const INITIAL_STATE = {

};

export const layoutReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
